# Sadaf Publisher deployment shell

This repository contains an encrypted production bundle for Sadaf Publisher.
The application source is encrypted; Railway receives the runtime key through a private environment variable.
